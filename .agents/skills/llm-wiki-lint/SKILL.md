---
name: llm-wiki-lint
description: "Use when operating schema-2 Knowledge OS. Run tests, source integrity, deterministic build, strict evidence lint, graph build, and public audit."
---
# llm-wiki-lint

Run tests, source integrity, deterministic build, strict evidence lint, graph build, and public audit.

Read `AGENTS.md` and the relevant `Schema/` contract first. Markdown is canonical. Never invent semantic content or citations in deterministic tooling. Verify with the maintenance gate before committing.
