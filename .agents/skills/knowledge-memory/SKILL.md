---
name: knowledge-memory
description: "Use when operating schema-2 Knowledge OS. Promote only meaningful context; confirm preferences; keep observations tentative; never use Memory as factual evidence."
---
# knowledge-memory

Promote only meaningful context; confirm preferences; keep observations tentative; never use Memory as factual evidence.

Read `AGENTS.md` and the relevant `Schema/` contract first. Markdown is canonical. Never invent semantic content or citations in deterministic tooling. Verify with the maintenance gate before committing.
