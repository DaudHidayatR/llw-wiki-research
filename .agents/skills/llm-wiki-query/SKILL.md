---
name: llm-wiki-query
description: "Use when operating schema-2 Knowledge OS. Search catalog first, expand explicit relations, include relevant research/decisions, and build context for complex queries."
---
# llm-wiki-query

Search catalog first, expand explicit relations, include relevant research/decisions, and build context for complex queries.

Read `AGENTS.md` and the relevant `Schema/` contract first. Markdown is canonical. Never invent semantic content or citations in deterministic tooling. Verify with the maintenance gate before committing.
