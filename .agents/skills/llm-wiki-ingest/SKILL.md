---
name: llm-wiki-ingest
description: "Use when operating schema-2 Knowledge OS. Hash/scan Raw, search compiled knowledge, create focused evidence-backed notes, then build/lint/accept coverage."
---
# llm-wiki-ingest

Hash/scan Raw, search compiled knowledge, create focused evidence-backed notes, then build/lint/accept coverage.

Read `AGENTS.md` and the relevant `Schema/` contract first. Markdown is canonical. Never invent semantic content or citations in deterministic tooling. Verify with the maintenance gate before committing.
