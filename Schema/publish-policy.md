# Publish Policy
Publishing is one-way. Allow only `Wiki/`, `Research/`, `Decisions/`, and `Welcome.md`. Deny Raw, Memory, Context, Schema, `.agents`, scripts, tests, environment state, binaries, secrets, and machine-local paths. `site-output/` is generated and noncanonical.
